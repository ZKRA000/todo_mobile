import 'package:flutter/material.dart';

ThemeData theme = ThemeData(
  primaryColor: const Color(0xFFDAF1F9),
);

class EventTag extends StatelessWidget {
  const EventTag({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('test'),
    );
  }
}
